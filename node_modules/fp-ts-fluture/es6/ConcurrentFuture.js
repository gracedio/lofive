/**
 * @since 0.5.0
 */
import * as F from 'fluture';
/**
 * @since 0.5.0
 */
export var URI = 'Fluture/ConcurrentFuture';
/**
 * @since 0.5.0
 */
export var concurrentFuture = {
    URI: URI,
    map: function (fa, f) { return F.map(f)(fa); },
    of: F.Par['fantasy-land/of'],
    ap: function (fab, fa) { return F.ap(fa)(fab); },
    alt: function (fx, f) { return F.alt(f())(fx); },
    zero: F.Par['fantasy-land/zero']
};
