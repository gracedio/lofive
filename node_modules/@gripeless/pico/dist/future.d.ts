import { FutureInstance } from 'fluture';
import { DetailedError } from './error';
export declare type Fluture<L, R> = FutureInstance<L, R>;
export declare const timeout: (ms: number) => <R>(fl: Fluture<DetailedError, R>) => Fluture<DetailedError, R>;
